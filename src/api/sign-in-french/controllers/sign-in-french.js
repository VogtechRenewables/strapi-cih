'use strict';

/**
 * sign-in-french controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sign-in-french.sign-in-french');
